/* Paste your Firebase Web app configuration from Firebase Console → Project settings → Your apps. */
export const firebaseConfig = null;

// Set this to the only Google account allowed to access your wedding plan.
export const allowedEmail = 'YOUR_GOOGLE_EMAIL@example.com';
export const planDocumentId = 'vaibhav-wedding-plan';
